JavaScript
(숫자)
▶ math.pow   ---> 제곱
ex) math.pow(3,2)  답: 9

▶ math.round   --->반올림
ex) math.round(10.6)  답: 11

▶ math.ceil   ---> 무조건 올림
ex) math.ceil (10.2)  답: 11

▶ math.floor   ---> 무조건 내림
ex) math.floor (10.2)  답: 10

▶ math.sqrt   ---> 제곱근
ex) math.sqrt (9)  답: 3

▶ math.random()   --->랜덤
ex) math.random()  답: 랜덤 숫자가 소수까지 나옴

▶ math.round(100*math.random());   --->랜덤
ex) 답: 소수자리는 안나오는 랜덤숫자

(문자)
▶ '   '    "    "   -----> 안에 들어가는것들은 다 문자로 인식!
ex) 
alert('coding everybody');
alert("coding everybody:);

▶ alert('minhee's coding everybody');
---> 이렇게 하고 싶으면 alert('minhee|'s coding everybody');   | 이걸 넣어줘야함


▶ typeof 1   ---> "number"
▶ typeof "1"   ---> "String"
▶ 문자를 여러줄로 표시하고 싶으면 "|n" 을 쓰면 된다
ex)
"1"+"1"   답: 11
"coding everybody".length
답: 16 // 문자 갯수가 16개여서임
"coding everybody".indexof("d")
답: 2
"coding everybody".indexof("o")
답: 1

(변수)
▶ 변수: (문자나 숫자 같은) 값을 담는 컨테이너로 값을 유지할 필요가 있을 때 사용한다. 여기서 담겨진 값은 다른 값으로 바꿀수 있다
키워드 : var

(비교)
▶ == : equal operator
동등 연산자로 좌항과 우항을 비교해서 서로 값이 같다면 true 다르면 false가 된다. '='가 두개인것을 주의하자
▶=== : strict operator
일치 연산자로 === 좌항과 우항이 '정확'하게 같을 때 true 다르면 false가 된다.
ex) 
alert(1=='1');  // true
alert(1==='1');  //false  데이터값까지 같아야 같다함

(조건문의 문법)
▶ if
(true) ---> if문 실행
(false) ---> if문 실행 못함
ex) if(true){
      alert('result : true');
            }
            
    if(false){
      alert('result : true');
            }
▶ else if
ex)
if(false){
      alert(1);
     }else if(true){
          alert(2);
          }
          
 ▶ prompt
 ------> 입력 받을수 있게 하는것.

▶ 함수
function 함수명([인자....[,인자]){
      코드
      return 반환값
      }
ex)
function numbering(){
      i = 0;
      while(i<10){
      document.write(i);
      i+=1;
      }
    }

▶ 배열 : 연관된 데이터를 모아서 통으로 관리하기 위해서 사용하는 데이터 타입이다.
ex)
function get_members(){
      return ['egoing','k8805','sorialgi'];
      }
  members= get_members();
  document.write(members[0]);
  답: egoing
  
  만약 출력 값을 대문자로 하고 싶으면...
  document.write(members[0].toUpperCase());
  
  ▶ 추가
  .push(' 추가하고픈거 넣기 ');
  
